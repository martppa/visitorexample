package com.martppa.visitor.servers.rest

import com.martppa.visitor.servers.RestServer

open class ApacheServer : RestServer {

    override fun init() {
        // Whatever a server needs to start
    }
}